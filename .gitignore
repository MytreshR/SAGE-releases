SAGE-releases hosts published builds only.
The application source lives in a separate private repository.
*.exe
*.dmg
*.AppImage
*.deb

# Reports written by tools/stats.mjs. Regenerate rather than commit:
# they are a snapshot of a moment and stale the day after.
sage-summary-*.csv
sage-daily-*.csv
